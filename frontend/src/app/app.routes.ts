/**
 * Archivo que define las rutas principales de la aplicación.
 * Cada ruta asocia una URL con un componente específico.
 * Angular Router usa esta configuración para navegar sin recargar la página.
 */

import { Routes } from '@angular/router';

export const routes: Routes = [];
